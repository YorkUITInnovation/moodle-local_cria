import React from 'react';
import ReactDOM from 'react-dom/client';
import LogsDashboard from './logsdashboard.jsx';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <LogsDashboard />
  </React.StrictMode>
);
